#!/usr/bin/zsh
# #!/usr/bin/zsh -xv
# or 
setopt XTRACE VERBOSE

# Have zsh flags to help with debugging
# -x = XTRACE to display each line after substitutions
# -v = VERBOSE to display each line before subs
print $HOME
